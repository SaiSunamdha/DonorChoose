flask==3.0.3
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
xgboost>=2.0 ; platform_system != "Windows" or python_version >= "3.10"
# gunicorn for Linux deployment; waitress for Windows (optional)
gunicorn>=21.2 ; platform_system != "Windows"
waitress>=3.0 ; platform_system == "Windows"
